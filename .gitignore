# Chaves reais do Supabase — nunca versionar.
# Copie config.example.js para config.js e preencha no servidor.
config.js

# Dependencias dos testes
node_modules/

# package-lock.json FICA versionado de proposito: a Hostinger roda `npm ci` na
# compilacao, e `npm ci` falha sem o lockfile.

# Lixo de sistema
Thumbs.db
.DS_Store
*.zip

# Segredos: senha do banco, do SMTP e a chave de sessao.
.env
.env.local
.env*.local

# Build e dependencias
node_modules/
.npm-cache/
.next/
next-env.d.ts
*.tsbuildinfo

# package-lock.json FICA versionado: a Hostinger roda `npm ci` na compilacao,
# e `npm ci` falha sem o lockfile.

# Lixo de sistema
Thumbs.db
.DS_Store
*.zip
